namespace TypesPlayer {
    export type ControlKeys = {
        up: string
        down: string
    }
    export type ControlKeysState = {
        up: boolean
        down: boolean
    }
}

export default TypesPlayer
